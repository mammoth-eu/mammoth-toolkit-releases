# PIPELINE DEFINITION
# Name: facex-embeddings
# Inputs:
#    dataset: system.Dataset
#    facex_embeddings__params: dict [Default: {'target_layer': 'None', 'target_class': 'None'}]
#    model: system.Model
#    sensitive: list
# Outputs:
#    output: system.HTML
components:
  comp-facex-embeddings:
    executorLabel: exec-facex-embeddings
    inputDefinitions:
      artifacts:
        dataset:
          artifactType:
            schemaTitle: system.Dataset
            schemaVersion: 0.0.1
        model:
          artifactType:
            schemaTitle: system.Model
            schemaVersion: 0.0.1
      parameters:
        facex_embeddings__params:
          defaultValue:
            target_class: None
            target_layer: None
          isOptional: true
          parameterType: STRUCT
        sensitive:
          parameterType: LIST
    outputDefinitions:
      artifacts:
        output:
          artifactType:
            schemaTitle: system.HTML
            schemaVersion: 0.0.1
deploymentSpec:
  executors:
    exec-facex-embeddings:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - facex_embeddings
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'mammoth-commons[deployment]'\
          \ 'torch' 'torchvision' 'timm' 'facextool' && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: mammotheu/facex_embeddings:v0037
pipelineInfo:
  name: facex-embeddings
root:
  dag:
    outputs:
      artifacts:
        output:
          artifactSelectors:
          - outputArtifactKey: output
            producerSubtask: facex-embeddings
    tasks:
      facex-embeddings:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-facex-embeddings
        inputs:
          artifacts:
            dataset:
              componentInputArtifact: dataset
            model:
              componentInputArtifact: model
          parameters:
            facex_embeddings__params:
              componentInputParameter: facex_embeddings__params
            sensitive:
              componentInputParameter: sensitive
        taskInfo:
          name: facex-embeddings
  inputDefinitions:
    artifacts:
      dataset:
        artifactType:
          schemaTitle: system.Dataset
          schemaVersion: 0.0.1
      model:
        artifactType:
          schemaTitle: system.Model
          schemaVersion: 0.0.1
    parameters:
      facex_embeddings__params:
        defaultValue:
          target_class: None
          target_layer: None
        isOptional: true
        parameterType: STRUCT
      sensitive:
        parameterType: LIST
  outputDefinitions:
    artifacts:
      output:
        artifactType:
          schemaTitle: system.HTML
          schemaVersion: 0.0.1
schemaVersion: 2.1.0
sdkVersion: kfp-2.12.1

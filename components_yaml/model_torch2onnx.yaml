# PIPELINE DEFINITION
# Name: model-torch2onnx
# Inputs:
#    model_torch2onnx__params: dict [Default: {'safe_libraries': 'numpy, torch, torchvision, PIL, io, requests', 'model_path': '', 'input_height': 224.0, 'model_name': 'model', 'state_path': '', 'multiclass_threshold': 0.0, 'input_width': 224.0}]
# Outputs:
#    output: system.Model
components:
  comp-model-torch2onnx:
    executorLabel: exec-model-torch2onnx
    inputDefinitions:
      parameters:
        model_torch2onnx__params:
          defaultValue:
            input_height: 224.0
            input_width: 224.0
            model_name: model
            model_path: ''
            multiclass_threshold: 0.0
            safe_libraries: numpy, torch, torchvision, PIL, io, requests
            state_path: ''
          isOptional: true
          parameterType: STRUCT
    outputDefinitions:
      artifacts:
        output:
          artifactType:
            schemaTitle: system.Model
            schemaVersion: 0.0.1
deploymentSpec:
  executors:
    exec-model-torch2onnx:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - model_torch2onnx
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'mammoth_commons[deployment]'\
          \ 'numpy>=2.0.0' 'torch' 'torchvision' && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: mammotheu/model_torch2onnx:v0049
pipelineInfo:
  name: model-torch2onnx
root:
  dag:
    outputs:
      artifacts:
        output:
          artifactSelectors:
          - outputArtifactKey: output
            producerSubtask: model-torch2onnx
    tasks:
      model-torch2onnx:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-model-torch2onnx
        inputs:
          parameters:
            model_torch2onnx__params:
              componentInputParameter: model_torch2onnx__params
        taskInfo:
          name: model-torch2onnx
  inputDefinitions:
    parameters:
      model_torch2onnx__params:
        defaultValue:
          input_height: 224.0
          input_width: 224.0
          model_name: model
          model_path: ''
          multiclass_threshold: 0.0
          safe_libraries: numpy, torch, torchvision, PIL, io, requests
          state_path: ''
        isOptional: true
        parameterType: STRUCT
  outputDefinitions:
    artifacts:
      output:
        artifactType:
          schemaTitle: system.Model
          schemaVersion: 0.0.1
schemaVersion: 2.1.0
sdkVersion: kfp-2.14.3

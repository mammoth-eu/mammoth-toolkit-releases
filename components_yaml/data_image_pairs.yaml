# PIPELINE DEFINITION
# Name: data-image-pairs
# Inputs:
#    data_image_pairs__params: dict [Default: {'image_root_dir': './', 'transform_variable': 'transform', 'path': '', 'data_transform_path': '', 'shuffle': False, 'batch_size': 4.0, 'target': '', 'safe_libraries': 'torchvision'}]
# Outputs:
#    output: system.Dataset
components:
  comp-data-image-pairs:
    executorLabel: exec-data-image-pairs
    inputDefinitions:
      parameters:
        data_image_pairs__params:
          defaultValue:
            batch_size: 4.0
            data_transform_path: ''
            image_root_dir: ./
            path: ''
            safe_libraries: torchvision
            shuffle: false
            target: ''
            transform_variable: transform
          isOptional: true
          parameterType: STRUCT
    outputDefinitions:
      artifacts:
        output:
          artifactType:
            schemaTitle: system.Dataset
            schemaVersion: 0.0.1
deploymentSpec:
  executors:
    exec-data-image-pairs:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - data_image_pairs
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'mammoth-commons[deployment]==0.0.38'\
          \ 'torch' 'torchvision' 'pandas' && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: mammotheu/data_image_pairs:v0036
pipelineInfo:
  name: data-image-pairs
root:
  dag:
    outputs:
      artifacts:
        output:
          artifactSelectors:
          - outputArtifactKey: output
            producerSubtask: data-image-pairs
    tasks:
      data-image-pairs:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-data-image-pairs
        inputs:
          parameters:
            data_image_pairs__params:
              componentInputParameter: data_image_pairs__params
        taskInfo:
          name: data-image-pairs
  inputDefinitions:
    parameters:
      data_image_pairs__params:
        defaultValue:
          batch_size: 4.0
          data_transform_path: ''
          image_root_dir: ./
          path: ''
          safe_libraries: torchvision
          shuffle: false
          target: ''
          transform_variable: transform
        isOptional: true
        parameterType: STRUCT
  outputDefinitions:
    artifacts:
      output:
        artifactType:
          schemaTitle: system.Dataset
          schemaVersion: 0.0.1
schemaVersion: 2.1.0
sdkVersion: kfp-2.11.0

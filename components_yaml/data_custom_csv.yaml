# PIPELINE DEFINITION
# Name: data-custom-csv
# Inputs:
#    data_custom_csv__params: dict [Default: {'path': '', 'skip_invalid_lines': True, 'categorical': 'None', 'delimiter': ',', 'numeric': 'None', 'label': 'None'}]
# Outputs:
#    Output: str
#    output: system.Dataset
components:
  comp-data-custom-csv:
    executorLabel: exec-data-custom-csv
    inputDefinitions:
      parameters:
        data_custom_csv__params:
          defaultValue:
            categorical: None
            delimiter: ','
            label: None
            numeric: None
            path: ''
            skip_invalid_lines: true
          isOptional: true
          parameterType: STRUCT
    outputDefinitions:
      artifacts:
        output:
          artifactType:
            schemaTitle: system.Dataset
            schemaVersion: 0.0.1
      parameters:
        Output:
          parameterType: STRING
deploymentSpec:
  executors:
    exec-data-custom-csv:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - data_custom_csv
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'mammoth-commons[deployment]'\
          \ 'fairbench' 'pandas' && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: mammotheu/data_custom_csv:v0024
pipelineInfo:
  name: data-custom-csv
root:
  dag:
    outputs:
      artifacts:
        output:
          artifactSelectors:
          - outputArtifactKey: output
            producerSubtask: data-custom-csv
      parameters:
        Output:
          valueFromParameter:
            outputParameterKey: Output
            producerSubtask: data-custom-csv
    tasks:
      data-custom-csv:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-data-custom-csv
        inputs:
          parameters:
            data_custom_csv__params:
              componentInputParameter: data_custom_csv__params
        taskInfo:
          name: data-custom-csv
  inputDefinitions:
    parameters:
      data_custom_csv__params:
        defaultValue:
          categorical: None
          delimiter: ','
          label: None
          numeric: None
          path: ''
          skip_invalid_lines: true
        isOptional: true
        parameterType: STRUCT
  outputDefinitions:
    artifacts:
      output:
        artifactType:
          schemaTitle: system.Dataset
          schemaVersion: 0.0.1
    parameters:
      Output:
        parameterType: STRING
schemaVersion: 2.1.0
sdkVersion: kfp-2.8.0

# PIPELINE DEFINITION
# Name: no-model
# Inputs:
#    no_model__params: dict [Default: {}]
# Outputs:
#    output: system.Model
components:
  comp-no-model:
    executorLabel: exec-no-model
    inputDefinitions:
      parameters:
        no_model__params:
          defaultValue: {}
          isOptional: true
          parameterType: STRUCT
    outputDefinitions:
      artifacts:
        output:
          artifactType:
            schemaTitle: system.Model
            schemaVersion: 0.0.1
deploymentSpec:
  executors:
    exec-no-model:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - no_model
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'mammoth-commons[deployment]==0.0.38'\
          \ && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: mammotheu/no_model:v0036
pipelineInfo:
  name: no-model
root:
  dag:
    outputs:
      artifacts:
        output:
          artifactSelectors:
          - outputArtifactKey: output
            producerSubtask: no-model
    tasks:
      no-model:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-no-model
        inputs:
          parameters:
            no_model__params:
              componentInputParameter: no_model__params
        taskInfo:
          name: no-model
  inputDefinitions:
    parameters:
      no_model__params:
        defaultValue: {}
        isOptional: true
        parameterType: STRUCT
  outputDefinitions:
    artifacts:
      output:
        artifactType:
          schemaTitle: system.Model
          schemaVersion: 0.0.1
schemaVersion: 2.1.0
sdkVersion: kfp-2.11.0

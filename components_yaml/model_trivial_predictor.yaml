# PIPELINE DEFINITION
# Name: model-trivial-predictor
# Inputs:
#    model_trivial_predictor__params: dict [Default: {}]
# Outputs:
#    output: system.Model
components:
  comp-model-trivial-predictor:
    executorLabel: exec-model-trivial-predictor
    inputDefinitions:
      parameters:
        model_trivial_predictor__params:
          defaultValue: {}
          isOptional: true
          parameterType: STRUCT
    outputDefinitions:
      artifacts:
        output:
          artifactType:
            schemaTitle: system.Model
            schemaVersion: 0.0.1
deploymentSpec:
  executors:
    exec-model-trivial-predictor:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - model_trivial_predictor
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'mammoth_commons[deployment]'\
          \ && \"$0\" \"$@\"\n"
        - python3
        - -m
        - kfp.dsl.executor_main
        image: mammotheu/model_trivial_predictor:v0049
pipelineInfo:
  name: model-trivial-predictor
root:
  dag:
    outputs:
      artifacts:
        output:
          artifactSelectors:
          - outputArtifactKey: output
            producerSubtask: model-trivial-predictor
    tasks:
      model-trivial-predictor:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-model-trivial-predictor
        inputs:
          parameters:
            model_trivial_predictor__params:
              componentInputParameter: model_trivial_predictor__params
        taskInfo:
          name: model-trivial-predictor
  inputDefinitions:
    parameters:
      model_trivial_predictor__params:
        defaultValue: {}
        isOptional: true
        parameterType: STRUCT
  outputDefinitions:
    artifacts:
      output:
        artifactType:
          schemaTitle: system.Model
          schemaVersion: 0.0.1
schemaVersion: 2.1.0
sdkVersion: kfp-2.14.3
